# ============================================================
# Private Key Manager - Environment Variables
# ============================================================
# Copy this file to .env and fill in the values
# NEVER commit .env to version control!
# ============================================================

# ------------------------------------------------------------
# PostgreSQL Database
# ------------------------------------------------------------
POSTGRES_USER=pkm_user
POSTGRES_PASSWORD=CHANGE_ME_STRONG_DB_PASSWORD
POSTGRES_DB=pkm_db

# ------------------------------------------------------------
# JWT Secrets
# Generate with: openssl rand -hex 64
# ------------------------------------------------------------
JWT_ACCESS_SECRET=CHANGE_ME_GENERATE_WITH_OPENSSL_RAND_HEX_64
JWT_REFRESH_SECRET=CHANGE_ME_GENERATE_WITH_OPENSSL_RAND_HEX_64

# ------------------------------------------------------------
# CORS - Comma-separated list of allowed origins
# ------------------------------------------------------------
ALLOWED_ORIGINS=https://yourdomain.com

# ------------------------------------------------------------
# Backend
# ------------------------------------------------------------
PORT=4000
NODE_ENV=production
LOG_LEVEL=info

# ------------------------------------------------------------
# Frontend (Vite build-time variables)
# ------------------------------------------------------------
VITE_API_URL=/api

# ------------------------------------------------------------
# Optional: Your domain for Let's Encrypt SSL
# ------------------------------------------------------------
DOMAIN=yourdomain.com
CERTBOT_EMAIL=admin@yourdomain.com
